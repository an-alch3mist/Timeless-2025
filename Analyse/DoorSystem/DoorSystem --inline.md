[pasted below prompt]

`UTIL.cs` , `DrawManager.cs` are read-only. the remaining are just for reference or 
provide a comprehensive First Person DoorSystem, 
keep the number of files(keep the files minimal),
make them seprate only if they monoBehaviour(make a certain script monoBehaviour only if required), if they are (non-static, static, interface, enum, events) keep them in a single file
feel free to critique, rewrite any thing as you seem fit.